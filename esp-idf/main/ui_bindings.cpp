#include "ui_bindings.h"
#include "esp_log.h"

static const char *TAG = "ui_bindings";

void ui_bindings_init() {
    ESP_LOGI(TAG, "UI bindings init (stub)");
    // TODO: Initialize LVGL + EEZ UI, set up global variables and widgets.
}

void ui_bindings_tick() {
    // TODO: Pump LVGL + EEZ tick handlers and apply UI updates.
}
